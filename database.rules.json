{
  "rules": {
     "profile": {
      "$user_id": {
        ".read": "$user_id === auth.uid",
        ".write": "$user_id === auth.uid"
      },
        "productsOrder":{
          "$user_id": {
        ".read": "$user_id === auth.uid",
        ".write": "$user_id === auth.uid"
      }
        },
            "FavProductsOrder":{
          "$user_id": {
        ".read": "$user_id === auth.uid",
        ".write": "$user_id === auth.uid"
      }
        }
    },
      "Game":{
     ".read": true,
    ".write": true
      },
    ".read": false,
    ".write": false
  }
}
